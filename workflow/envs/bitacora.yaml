name: bitacora
channels:
  - conda-forge
  - bioconda
dependencies:
  - blast          # BLAST+ (tblastn/blastp/makeblastdb)
  - hmmer          # perfiles HMM (hmmsearch/hmmbuild)
  - perl
  # - gemoma       # descomentar solo si en algun caso usas GEMOMA=T
